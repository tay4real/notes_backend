{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../notes && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git comit -m uibuild && git push"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tay4real"
  },
  "author": "Ademuyiwa Otubusin",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  }
}
